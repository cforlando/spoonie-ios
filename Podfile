# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Daily Spoonie' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Daily Spoonie
  pod 'RealmSwift'
  pod 'Charts'

  target 'Daily SpoonieTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Daily SpoonieUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
